# Deployment

Deploy the stateless Next.js application with environment-based configuration and managed MySQL. Run migrations as a release step. Store images through the ImageStorage contract: local development storage first, object storage in cloud environments. Add health checks, structured logs, backups, and secret management before production.
