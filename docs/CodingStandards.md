# Coding Standards

Use strict TypeScript, explicit public return types, descriptive names, small modules, and immutable data where practical. Prefer server components by default. Validate boundaries with Zod and keep functions focused.
