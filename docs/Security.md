# Security

Validate input with Zod, use Sequelize parameterization, encode output, and use secure HTTP-only cookies for eventual sessions. Enforce server-side authorization, CSRF defenses for cookie-authenticated mutations, rate limits for sensitive endpoints, least-privilege database accounts, secret-only environment variables, and audit logs for privileged actions.
