# Folder Structure

```text
src/
  app/             # routes, layouts, metadata, route handlers
  application/     # cross-feature use cases
  config/          # validated configuration
  domain/          # business entities and contracts
  features/        # cohesive feature modules
  infrastructure/  # Sequelize, storage, external services
  shared/          # reusable UI and utilities
```
