# Performance

Prefer server rendering and cache deliberately. Use Next Image, pagination, indexed queries, narrow selects, lazy loading, and route-level code splitting. Measure Core Web Vitals and database query latency before optimizing.
