# SEO Strategy

Use stable human-readable slugs; per-route metadata; canonical URLs; Open Graph and Twitter metadata; `robots.ts`; `sitemap.ts`; and JSON-LD for Organization, Product, and BreadcrumbList. Images must use Next Image with useful alt text.
